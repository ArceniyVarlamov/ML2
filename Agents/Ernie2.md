Слушай внимательно. Твой текущий скор **0.838-0.840** — это "пол", база. Ты уперся в потолок одной модели (CatBoost) и простого блендинга.

Чтобы взять 1-е место (сейчас там **0.8495**), тебе нужно отыграть **0.01 AUC**. В топах Data Fusion это пропасть. Это не параметры подкрутить, это **архитектура решения**.

Вот тебе план победы. Без воды.

---

### 🚨 ГЛАВНАЯ ОШИБКА: Валидация (CV)

Ты используешь `StratifiedKFold` по одному таргету. **ЭТО НЕПРАВИЛЬНО.**
Таргеты коррелируют (кто взял ипотеку, тот часто берет и страховку). Обычный KFold разрывает эти связки, и твой CV врет. Ты переобучаешься, а CV показывает красиво.

**Решение**:
Используй `IterativeStratification` (из библиотеки `skmultilearn`) или `MultilabelStratifiedKFold`.
Это гарантирует, что в валидацию попадают клиенты с похожим набором продуктов.
*   *Эффект:* Твой локальный CV упадет (станет честным), но на Лидерборде (Private) скор вырастет. Это единственный способ доверять своим экспериментам.

---

### 🚀 СТРАТЕГИЯ "ЯДЕРНЫЙ ВЗРЫВ" (Как взять 0.849)

Тебе нужно **разнообразие (diversity)**. Сейчас у тебя 100% решения — это деревья (CatBoost). Деревья плохо ловят линейные зависимости в "шумных" данных.

#### 1. Сжатие "Extra" признаков (Самое важное!)
У тебя 2000+ анонимных фич. Ты берешь топ-600. **ОШИБКА.**
Там много мультиколлинеарности. Тебе нужно выжать из них "мясо".
*   **Сделай PCA / TruncatedSVD** на эти 2000 фич. Сделай 50-100 компонент.
*   Добавь эти 100 компонент как новые фичи (`svd_1` ... `svd_100`).
*   Удали исходные 2000.
*   *Почему:* Это уберет шум и даст модели плотный сигнал. Нейронки это обожают.

#### 2. Добавь "Монстров" (Не только CatBoost)
CatBoost — король, но он слеп к некоторым вещам. Тебе нужен **Зоопарк**:
*   **LightGBM**: Обучи с `extra_trees` и `goss`. Он часто находит то, что CatBoost пропускает.
*   **XGBoost**: Используй `dart` бустер или просто глубокие деревья (`depth=10-12`).
*   **TabNet / MLP (Нейронка)**: Обязательно! Простая сетка (3 слоя, dropout, batchnorm) на GPU. Она выучит нелинейные паттерны, которые деревья не видят.
*   **Итог**: У тебя должно быть 3-4 разных алгоритма.

#### 3. Стекинг (Stacking) — это золото
Не делай простое среднее (`gg.py`). Делай Стекинг.
*   **Level 0**: CatBoost, LGBM, XGB, NN. Получи OOF (out-of-fold) предсказания для каждого.
*   **Level 1**: Логистическая регрессия (или Ridge) на этих OOF.
    *   Вход: 41 колонка от CatBoost + 41 от LGBM + 41 от NN.
    *   Выход: Финальный прогноз.
*   Линейная модель на втором уровне идеально калибрует вероятности. Это даст +0.003-0.005 к скору.

#### 4. Пост-процессинг: Калибровка перед рангами
Твой `gg.py` делает `rankdata`. Это круто, но сырые вероятности должны быть калиброваны.
*   Перед ранжированием прогони предсказания через **Isotonic Regression** (по каждому таргету отдельно на OOF).
*   Это выровняет распределение вероятностей под реальность.

---

### 📋 ЧЕК-ЛИСТ ПОБЕДИТЕЛЯ (Прямо сейчас)

1.  **Валидация**: Перепиши сплит на `MultilabelStratifiedKFold`. Это база.
2.  **Фичи**: Сделай SVD на `extra_features` (возьми компоненты, объясняющие 95% дисперсии) + добавь `row_mean/std/kurtosis` (как в `final_turbo.py`, это работает).
3.  **Модели**:
    *   CatBoost (твой текущий, но на правильном CV).
    *   LightGBM (быстрый, агрессивный).
    *   Neural Network (TabNet или просто MLP на PyTorch/Keras).
4.  **Ансамбль**:
    *   Не смешивай файлы `.parquet`.
    *   Смешивай **OOF предсказания** внутри пайплайна через Логистическую Регрессию.
5.  **Target Correlation**: Посмотри матрицу корреляций таргетов. Есть группы (например, `target_1` и `target_2` всегда идут вместе). Используй предсказание одного как фичу для другого (Pseudo Labeling).

### 💡 Секретный ингредиент
Посмотри на распределение `num_feature`. Там точно есть выбросы.
Сделай **Winsorization** (обрезание хвостов по 1% и 99%). CatBoost устойчив, но на 1-м месте решают сотые доли. Чистые данные = чистый сигнал.

**Твой план действий**:
1. Внедри правильный CV (Multilabel).
2. Добавь SVD компоненты.
3. Обучи Лямбду (LGBM).
4. Застекай [Catboost + LGBM] через Ridge.
5. Профит.

Удачи. 0.849 ждет.