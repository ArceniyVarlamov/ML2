

# Анализ ситуации и план выхода на 1 место

## Текущий разрыв

| Позиция | Score | Δ от лидера |
|---------|-------|-------------|
| 🥇 Лидер | 0.8495 | — |
| 🏠 Мы | 0.8384 | **−0.0111** |

0.011 — это **существенный** разрыв. Rank-блендинг двух почти одинаковых CatBoost-моделей даёт копейки. Нужны **структурные изменения**.

---

## Диагностика проблем текущих пайплайнов

1. **Нулевое разнообразие моделей** — оба пайплайна это CatBoost с почти идентичными параметрами. Блендить их бессмысленно.
2. **Минимальный feature engineering** — только `row_mean`, `row_zeros`, `row_max`. Это ~0 информации.
3. **Используется 600 из 2000+ extra-фичей** — потенциально выкидываем ключевые сигналы.
4. **Нет per-target оптимизации** — один набор гиперпараметров для всех 41 таргетов.
5. **Нет эксплуатации корреляций между таргетами** — multi-label структура игнорируется.
6. **Нет стекинга** — OOF сохраняются, но не используются.

---

## Стратегический план (3 уровня прироста)

### Уровень 1: Diversity + Ensemble → ожидаемый +0.004–0.006

```
CatBoost (текущий ~0.838)
+ LightGBM  (~0.836-0.840)  
+ XGBoost   (~0.834-0.838)
= Rank Blend → ~0.842-0.844
```

### Уровень 2: Feature Engineering → ожидаемый +0.003–0.005

```
- PCA/SVD на 2000+ extra features (50-100 компонент)
- Target encoding категорий
- Pairwise interactions top-фичей
- Frequency encoding категорий
- Null-pattern features
```

### Уровень 3: Stacking + Target correlations → ожидаемый +0.002–0.003

```
- Level-2 модель на OOF предсказаниях
- Multi-output учёт корреляций
```

## Порядок действий (приоритет)

| # | Действие | Ожидаемый прирост | Время |
|---|----------|-------------------|-------|
| 1 | **LightGBM pipeline** (lgbm_pipeline.py) | +0.002–0.004 в бленде | 6–10ч |
| 2 | **XGBoost pipeline** (xgb_pipeline.py) | +0.001–0.002 в бленде | 6–10ч |
| 3 | **Оптимальный ансамбль** (smart_ensemble.py) | +0.002–0.003 | 5 мин |
| 4 | **PCA/SVD фичи** → пересчёт всех моделей | +0.002–0.004 | +50% время |
| 5 | **Per-target feature selection** | +0.001–0.002 | x41 overhead |

**Шаги 1–3 — приоритет #1.** Разнообразие моделей + умный ансамбль — это 70% успеха в табличных соревнованиях.